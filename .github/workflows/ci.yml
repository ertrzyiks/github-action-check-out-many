name: CI
on:
  push:
    branches: [main]

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ./.github/actions/check-out-many
        with:
          repos: |
            ertrzyiks/amxx-canvas
            ertrzyiks/react-native-component-test

